import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.CxcmDv9y.js";var i=JSON.parse(`{"title":"auth","description":"","frontmatter":{},"headers":[],"relativePath":"reference/server/http/middlewares/auth.md","filePath":"reference/server/http/middlewares/auth.md","lastUpdated":1787645887000}`),a={name:`reference/server/http/middlewares/auth.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="auth" tabindex="-1">auth <a class="header-anchor" href="#auth" aria-label="Permalink to “auth”">​</a></h1><p><code>auth</code> is the PostgreSQL-backed authentication middleware. It replaces the legacy <a href="./iam.html"><code>iam</code></a> + <a href="./oauth2.html"><code>oauth2</code></a> stack with one middleware that serves IAM, OAuth2, LDAP sync, and an embedded management UI.</p><p>The middleware behaves like a standalone app with its own UI: every runtime setting (OAuth2 redirect behavior, permission check rules, cache polling, token lifetimes, OAuth clients/providers, LDAP) lives in PostgreSQL and is managed through the API or UI. The static configuration only covers how to reach that database: encryption key, database connection, and migration settings.</p><p>Reads are served from an in-memory read model; writes go to PostgreSQL inside a transaction that bumps a version, records an event, and emits <code>pg_notify(&#39;auth_changed&#39;, version)</code>. Other instances pick up changes through version polling.</p><div class="language-yaml"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  http</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    middlewares</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      auth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">        auth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">          prefix_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/auth</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">          database</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">            dsn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">postgres://turna:turna@localhost:5432/turna?sslmode=disable</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">          encryption</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">            key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\${AUTH_ENCRYPTION_KEY}</span></span></code></pre></div><h2 id="fields" tabindex="-1">Fields <a class="header-anchor" href="#fields" aria-label="Permalink to “Fields”">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><code>prefix_path</code></td><td>Base path for Auth API and UI. Defaults to <code>/auth</code>.</td></tr><tr><td><code>database.dsn</code></td><td>PostgreSQL connection string. Required.</td></tr><tr><td><code>database.max_open_conns</code></td><td><code>database/sql</code> max open connection limit. Defaults to <code>5</code>; negative for unlimited.</td></tr><tr><td><code>database.max_idle_conns</code></td><td><code>database/sql</code> max idle connection limit. Defaults to <code>3</code>; negative for none.</td></tr><tr><td><code>database.conn_max_lifetime</code></td><td>Connection max lifetime. Defaults to <code>15m</code>; negative for unlimited.</td></tr><tr><td><code>database.conn_max_idle_time</code></td><td>Optional connection max idle time.</td></tr><tr><td><code>database.migration.dsn</code></td><td>Optional DSN used only while running migrations (e.g. a user with DDL privileges). Defaults to <code>database.dsn</code>; the connection is closed after migration.</td></tr><tr><td><code>database.migration.disabled</code></td><td>Disable built-in migration bootstrap.</td></tr><tr><td><code>database.migration.values</code></td><td>Optional <code>muz.Migrate</code> template substitution values.</td></tr><tr><td><code>database.migration.table</code></td><td>Migration tracking table. Defaults to <code>auth_migrations</code>.</td></tr><tr><td><code>database.migration.lock_key</code></td><td>PostgreSQL advisory lock key. Defaults to <code>muz:postgres:turna:auth_migrations</code>.</td></tr><tr><td><code>encryption.key</code></td><td>Required encryption key for secrets stored in PostgreSQL. Raw strings are SHA-256 derived; base64 16/24/32-byte keys are used directly.</td></tr><tr><td><code>ldap.disable_sync</code></td><td>Keep this instance out of the periodic LDAP sync loop; the manual sync API keeps working. Config-file only. Instances that do participate coordinate through the <code>auth_sync_locks</code> table, so a fleet sharing one database syncs once per <code>sync_duration</code> instead of once per instance.</td></tr></tbody></table><h2 id="runtime-settings-stored-in-postgresql" tabindex="-1">Runtime settings (stored in PostgreSQL) <a class="header-anchor" href="#runtime-settings-stored-in-postgresql" aria-label="Permalink to “Runtime settings (stored in PostgreSQL)”">​</a></h2><p>Everything else is a settings namespace under <code>/auth/v1/settings/{namespace}</code> and takes effect without a restart. The UI exposes dedicated pages for OAuth2, access checks, API keys, email and mTLS; the <em>Runtime Settings</em> page keeps the remaining operational namespaces.</p><table tabindex="0"><thead><tr><th>Namespace</th><th>Keys</th><th>Description</th></tr></thead><tbody><tr><td><code>admin</code></td><td><code>permission</code>, <code>allow_missing_x_user</code></td><td>Management API/UI authorization. Empty <code>permission</code> keeps bootstrap-open behavior. When set, <code>X-User</code> must have that permission ID/name. <code>allow_missing_x_user</code> defaults to <code>true</code> for break-glass access when the session chain is removed and no <code>X-User</code> is present. Do not expose this route publicly when break-glass is enabled.</td></tr><tr><td><code>oauth2</code></td><td><code>base_url</code>, <code>schema</code>, <code>insecure_skip_verify</code></td><td>Code-flow redirect behavior for upstream providers and the canonical token issuer. <code>schema</code> defaults to <code>https</code>. Set <code>base_url</code> when one auth instance serves session/login flows through multiple hosts, so a token issued through one host can be refreshed through another without an issuer mismatch.</td></tr><tr><td><code>check</code></td><td><code>default_hosts</code>, <code>no_host_check</code></td><td>Host rules for permission evaluation.</td></tr><tr><td><code>cache</code></td><td><code>poll_interval</code>, <code>code_store</code></td><td>Version poll interval for the in-memory read model and OAuth2 temporary code/state store. <code>code_store.active</code> is <code>memory</code> or <code>redis</code>.</td></tr><tr><td><code>token</code></td><td><code>token_lifetime</code>, <code>refresh_lifetime</code>, <code>refresh_absolute_lifetime</code></td><td>Access lifetime, refresh idle window and remembered-session ceiling (defaults <code>15m</code> / <code>24h</code> / <code>720h</code>).</td></tr><tr><td><code>jwt</code></td><td><code>kid</code>, <code>private_key</code></td><td>RS256 signing key (PEM, PKCS#8 or PKCS#1); auto-generated on first start. Editable through the API/UI and applied without restart — the public JWKS key is derived from the private key. Changing or rotating the key invalidates outstanding tokens.</td></tr><tr><td><code>passkey</code></td><td><code>disabled</code>, <code>rp_id</code>, <code>rp_display_name</code>, <code>origins</code>, <code>user_verification</code></td><td>WebAuthn (passkey) relying party settings. Empty <code>rp_id</code> defaults to the registrable domain (eTLD+1) of the request host — e.g. <code>auth.example.com</code> becomes <code>example.com</code>, so one passkey works across all subdomains; IPs and single-label hosts (<code>localhost</code>) are used as-is. Empty <code>origins</code> derives from the forwarded scheme + host.</td></tr><tr><td><code>password</code></td><td><code>disabled</code>, <code>local_disabled</code>, <code>ldap_disabled</code>, <code>ldap_register_disabled</code></td><td>Password grant sources. Defaults keep the implicit behavior: local users check bcrypt, non-local users bind against LDAP, unknown aliases are auto-created from LDAP on first login.</td></tr><tr><td><code>api_key</code></td><td><code>disabled</code>, <code>self_service</code>, <code>max_lifetime</code></td><td>Static API key creation and validation. <code>self_service</code> (default off) lets any authenticated X-User issue and manage their own keys through <code>/v1/api-keys</code> — a &quot;Personal access keys&quot; panel appears on the account page. <code>max_lifetime</code> caps the expiry of new keys (duration string); empty means keys may live forever.</td></tr><tr><td><code>device</code></td><td><code>disabled</code>, <code>code_lifetime</code>, <code>interval</code>, <code>verification_uri</code></td><td>RFC 8628 device flow. Defaults: codes live <code>10m</code>, minimum poll interval <code>5</code> seconds, verification URI <code>&lt;prefix&gt;/ui/device</code>.</td></tr><tr><td><code>token_exchange</code></td><td><code>disabled</code></td><td>RFC 8693 token exchange grant.</td></tr><tr><td><code>totp</code></td><td><code>disabled</code>, <code>issuer</code>, <code>skew</code></td><td>TOTP second factor. <code>issuer</code> is shown in authenticator apps (default <code>Turna Auth</code>), <code>skew</code> is the allowed period drift (default <code>1</code> = ±30s). Confirming TOTP also issues 8 single-use recovery codes.</td></tr><tr><td><code>email</code></td><td><code>disabled</code>, <code>magic_link</code>, <code>from</code>, <code>subject</code>, <code>body_template</code>, <code>magic_link_subject</code>, <code>magic_link_body_template</code>, <code>code_lifetime</code>, <code>smtp.{host,port,username,password,no_auth,starttls,tls,insecure_skip_verify}</code></td><td>Passwordless email login with two independent mails: the one-time code (<code>disabled</code>, <code>subject</code>, <code>body_template</code>) and the magic link (<code>magic_link</code> default true, <code>magic_link_subject</code>, <code>magic_link_body_template</code>). All templates are Go <code>text/template</code> strings; empty uses built-in defaults. Set <code>smtp.no_auth=true</code> for trusted relays that need no authentication. Codes live <code>15m</code> by default. The relay is also used by <code>signup</code>. Login is effectively off until <code>smtp.host</code> is set.</td></tr><tr><td><code>signup</code></td><td><code>enabled</code>, <code>email_verification</code>, <code>password_reset</code>, <code>default_role_ids</code>, <code>code_lifetime</code>, <code>verify_subject</code>, <code>verify_body_template</code>, <code>reset_subject</code>, <code>reset_body_template</code></td><td>Self-registration and forgot-password flows (UI: <em>Signup</em>). Off by default. <code>email_verification</code> defaults to <code>true</code>; verification/reset mails use the <code>email</code> SMTP relay. Codes live <code>1h</code> by default. Templates are Go <code>text/template</code> strings validated on save.</td></tr><tr><td><code>mtls</code></td><td><code>enabled</code>, <code>cert_header</code></td><td>Certificate based client authentication (RFC 8705 style). <code>cert_header</code> names a trusted proxy header carrying the client certificate (e.g. nginx <code>$ssl_client_escaped_cert</code>); only set it behind a trusted proxy. Off by default.</td></tr><tr><td><code>saml</code></td><td><code>certificate</code>, <code>private_key</code></td><td>SAML SP signing key pair; auto-generated (self-signed, 10 years) on first SAML use.</td></tr><tr><td><code>authorize</code></td><td><code>disabled</code>, <code>flow_lifetime</code>, <code>login_url</code></td><td>Local browser-based authorization code flow (<code>/oauth2/authorize</code> + consent screen). Pending consents live <code>10m</code> by default. <code>login_url</code> redirects anonymous browsers to a login page (with <code>?redirect_path=</code> back reference, the <a href="./login.html"><code>login</code></a> middleware convention); empty shows an error asking to log in first.</td></tr><tr><td><code>registration</code></td><td><code>enabled</code>, <code>client_lifetime</code>, <code>default_scope</code>, <code>max_clients</code></td><td>RFC 7591 dynamic client registration (<code>/oauth2/register</code>; UI: <em>OAuth2 → Dynamic client registration</em>). Off by default because registration is anonymous. <code>client_lifetime</code> expires dynamic clients (empty keeps them forever), <code>max_clients</code> caps stored dynamic clients (default <code>1000</code>).</td></tr><tr><td><code>custom_info</code></td><td><code>disabled</code>, <code>sets</code></td><td>Per-name userinfo claim templates served at <code>/oauth2/userinfo/{custom}</code> (and advertised by <code>/oauth2/openid/{custom}/.well-known/openid-configuration</code>). <code>sets.&lt;name&gt;.claims</code> maps an output claim to a Go <code>text/template</code>; templates receive <code>{{ .claims }}</code> (the base userinfo claims) and <code>{{ .user }}</code> (the full user record). A template whose key is new adds a claim, an existing key overwrites it, and a template that renders empty (e.g. <code>&quot;&quot;</code> or trimmed with <code>{{- -}}</code>) removes the claim. Templates are validated on save. Managed in the UI under <em>Custom Info</em>.</td></tr><tr><td><code>session_providers</code></td><td><code>providers</code></td><td>A <a href="./session.html"><code>session</code></a> middleware provider list managed from the UI (<em>Federation → Session providers</em>) instead of static YAML. <code>providers</code> is a map keyed by provider name using the session middleware&#39;s provider model (<code>name</code>, <code>auth_middleware</code>, <code>oauth2.*</code>, <code>passkey</code>, <code>password_flow</code>, <code>api_key</code>, <code>x_user</code>, <code>claim_header</code>, <code>priority</code>, <code>hide</code>, ...). A session middleware pulls it with <code>provider_source.auth_middleware</code> (in-process, applied on the next request after a commit) or over <code>GET /v1/session-providers</code> with <code>provider_source.url</code> (remote, TTL polled). Dynamic providers override same-named static ones.</td></tr></tbody></table><p>Example:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> PUT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /auth/v1/settings/oauth2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;value&quot;:{&quot;schema&quot;:&quot;https&quot;,&quot;base_url&quot;:&quot;&quot;}}&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> PUT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /auth/v1/settings/check</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;value&quot;:{&quot;no_host_check&quot;:true}}&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> PUT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /auth/v1/settings/cache</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;value&quot;:{&quot;poll_interval&quot;:&quot;5s&quot;,&quot;code_store&quot;:{&quot;active&quot;:&quot;redis&quot;,&quot;redis&quot;:{&quot;address&quot;:[&quot;redis:6379&quot;]}}}}&#39;</span></span></code></pre></div><h2 id="storage" tabindex="-1">Storage <a class="header-anchor" href="#storage" aria-label="Permalink to “Storage”">​</a></h2><p>Migrations are embedded and run through <code>github.com/rakunlabs/muz</code> with a PostgreSQL advisory lock. The schema includes:</p><ul><li><code>auth_versions</code>, <code>auth_events</code> — monotonic change version and durable event log.</li><li><code>auth_settings</code> — encrypted JSON settings namespaces. Reserved: <code>admin</code>, <code>jwt</code>, <code>token</code>, <code>oauth2</code>, <code>check</code>, <code>cache</code>, <code>api_key</code>, <code>device</code>, <code>token_exchange</code>, <code>totp</code>, <code>email</code>, <code>signup</code>, <code>mtls</code>, <code>saml</code>, <code>custom_info</code>, <code>authorize</code>, <code>registration</code>, <code>session_providers</code> (see <a href="#runtime-settings-stored-in-postgresql">Runtime settings</a>).</li><li><code>auth_oauth_clients</code>, <code>auth_oauth_providers</code>, <code>auth_ldap_configs</code>, <code>auth_saml_providers</code> — encrypted config records.</li><li><code>auth_users</code> — IAM users; the <code>details</code> map is encrypted at rest, passwords are bcrypt hashed.</li><li><code>auth_roles</code>, <code>auth_permissions</code>, <code>auth_lmaps</code> — IAM model.</li><li><code>auth_api_keys</code> — api keys (sha256 hashes; the key itself is never stored).</li><li><code>auth_totp_secrets</code> — encrypted TOTP shared secrets.</li><li><code>auth_flow_codes</code> — short-lived flow state shared between instances (device codes, email login codes, SAML relay states, pending consents, revoked token ids).</li></ul><h2 id="routes" tabindex="-1">Routes <a class="header-anchor" href="#routes" aria-label="Permalink to “Routes”">​</a></h2><p>With <code>prefix_path: /auth</code>:</p><h3 id="management-ui" tabindex="-1">Management UI <a class="header-anchor" href="#management-ui" aria-label="Permalink to “Management UI”">​</a></h3><table tabindex="0"><thead><tr><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>/auth/ui/*</code></td><td>Embedded Svelte management UI (users, roles, permissions, LDAP, OAuth, settings, API keys, email, mTLS, self-service account).</td></tr><tr><td><code>/auth/ui/#account</code></td><td>Self-service account page for the current <code>X-User</code>: password, TOTP recovery, passkeys, roles and permissions. With <code>api_key.self_service</code> on it also carries a &quot;Personal access keys&quot; panel for issuing and revoking own API keys.</td></tr><tr><td><code>/auth/ui/#api-keys</code></td><td>Admin API key principal management: choose an owner user/service account, create with a lifetime, attach role/permission IDs, copy the one-time key, list/update/revoke existing keys, and edit API key runtime settings.</td></tr><tr><td><code>/auth/ui/#email</code></td><td>Email code login settings: SMTP relay, code mail Go-template subject/body editor and preview.</td></tr><tr><td><code>/auth/ui/#magic-link</code></td><td>Magic link login settings: enable toggle, magic link mail template editor and preview (shares the <code>email</code> SMTP relay).</td></tr><tr><td><code>/auth/ui/#signup</code></td><td>Self-registration settings: signup/verification/password-reset toggles, default roles, code lifetime, mail template editors with preview.</td></tr><tr><td><code>/auth/ui/#oauth2-overview</code></td><td>OAuth2 token, authorization, dynamic client registration, password/passkey, and signing-key settings.</td></tr><tr><td><code>/auth/ui/device?user_code=XXXX-XXXX</code></td><td>RFC 8628 device approval/deny page; <code>user_code</code> is optional and pre-fills the form when present.</td></tr><tr><td><code>/auth/ui/#mtls</code></td><td>Global mTLS settings and workflow guide; certificate bindings live on service account records.</td></tr><tr><td><code>/auth/ui/#custom-info</code></td><td>Custom userinfo template sets: per-name claim template editor (add/overwrite/remove claims) with a live preview against sample claims and user details.</td></tr><tr><td><code>/auth/ui/#session-providers</code></td><td>Session middleware provider list (<code>session_providers</code> namespace): per-provider editor (auth middleware binding, OAuth2 endpoints, passkey/password/API key toggles, claim headers) with wiring examples for <code>provider_source</code>.</td></tr><tr><td><code>/auth/swagger/*</code></td><td>Swagger UI for the auth API (served with the ada swagger handler; spec at <code>/auth/swagger/swagger.json</code>).</td></tr></tbody></table><h3 id="iam" tabindex="-1">IAM <a class="header-anchor" href="#iam" aria-label="Permalink to “IAM”">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET/POST</code></td><td><code>/auth/v1/users</code></td><td>List/create users.</td></tr><tr><td><code>GET/PUT/PATCH/DELETE</code></td><td><code>/auth/v1/users/{id}</code></td><td>Manage one user.</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/users/{id}/access</code></td><td>Grant/remove temporary roles or permissions.</td></tr><tr><td><code>GET/POST</code></td><td><code>/auth/v1/service-accounts</code></td><td>List/create service accounts.</td></tr><tr><td><code>GET/PUT/PATCH/DELETE</code></td><td><code>/auth/v1/service-accounts/{id}</code></td><td>Manage one service account.</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/service-accounts/{id}/access</code></td><td>Temporary access for service accounts.</td></tr><tr><td><code>GET/POST</code></td><td><code>/auth/v1/roles</code></td><td>List/create roles.</td></tr><tr><td><code>GET/PUT/PATCH/DELETE</code></td><td><code>/auth/v1/roles/{id}</code></td><td>Manage one role.</td></tr><tr><td><code>GET/POST</code></td><td><code>/auth/v1/permissions</code></td><td>List/create permissions.</td></tr><tr><td><code>GET/PUT/PATCH/DELETE</code></td><td><code>/auth/v1/permissions/{id}</code></td><td>Manage one permission.</td></tr><tr><td><code>GET/POST</code></td><td><code>/auth/v1/lmaps</code></td><td>List/create LDAP maps.</td></tr><tr><td><code>GET/PUT/DELETE</code></td><td><code>/auth/v1/lmaps/{name}</code></td><td>Manage one LDAP map.</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/check</code></td><td>Permission check by optional alias/id + host/path/method. Without identity, checks public permissions only.</td></tr><tr><td><code>POST</code></td><td><code>/auth/check</code></td><td>Permission check for the <code>X-User</code> header identity. Without <code>X-User</code>, a public match returns allowed; a private request returns 401.</td></tr><tr><td><code>GET</code></td><td><code>/auth/info</code></td><td>Identity info for the <code>X-User</code> header.</td></tr><tr><td><code>GET</code></td><td><code>/auth/v1/dashboard</code></td><td>Totals and extended roles.</td></tr></tbody></table><p>List endpoints parse the query string with <a href="https://pkg.go.dev/github.com/rakunlabs/query" target="_blank" rel="noreferrer"><code>rakunlabs/query</code></a>: use <code>_limit</code>/<code>_offset</code> for paging (legacy <code>limit</code>/<code>offset</code> keys still work) plus field filters such as <code>name=...</code>, <code>role_ids=...</code>, <code>add_roles=true</code>.</p><h4 id="public-permission-resources" tabindex="-1">Public permission resources <a class="header-anchor" href="#public-permission-resources" aria-label="Permalink to “Public permission resources”">​</a></h4><p>A permission can be marked <code>public: true</code> in the permission editor or API. Its resource list then applies to everyone, including requests without <code>X-User</code>. It uses the ordinary permission matcher: <code>hosts</code>, doublestar <code>paths</code>, case-insensitive <code>methods</code>, and <code>excluded</code> resources all continue to apply.</p><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;public-pages&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;public&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;resources&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;hosts&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;app.example.com&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;paths&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/health&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/docs/**&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;methods&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GET&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;excluded&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        {</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;paths&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/docs/internal/**&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;methods&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GET&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A <a href="./session.html"><code>session</code></a> middleware in front picks these up when this auth is listed in its <code>auth_skip_paths</code>: anonymous requests matching a public permission pass through session with identity headers stripped instead of being redirected to login (remote deployments list the check endpoint URL instead of the name). Alternatively open the route by hand with session <code>skip_paths</code>. Behind session, <code>iam_check</code> performs the same check — directly with <code>auth_middleware: &lt;name&gt;</code> (recommended) or via the remote check API — and passes the request only when a public permission matches. Authenticated users also get public access without having the permission assigned through a role.</p><h3 id="ldap" tabindex="-1">LDAP <a class="header-anchor" href="#ldap" aria-label="Permalink to “LDAP”">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/auth/v1/ldap/groups</code></td><td>List groups from the active LDAP.</td></tr><tr><td><code>GET</code></td><td><code>/auth/v1/ldap/users/{uid}</code></td><td>Fetch one LDAP user.</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/ldap/sync</code></td><td>Sync all LDAP groups/users.</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/ldap/sync/{uid}</code></td><td>Sync one LDAP user.</td></tr></tbody></table><p>The active LDAP config is the first enabled record under <code>/auth/v1/ldap/configs</code>. A background loop syncs on <code>sync_duration</code> (default <code>10m</code>) unless <code>disable_sync</code> is set.</p><p>Automatic group mapping (same model as the legacy <code>iam</code> middleware):</p><ol><li>On every sync, each LDAP group missing from the group maps gets a <strong>role with the same name</strong> (created when missing) and a group map (<code>lmap</code>) pointing to that role.</li><li>Group members receive all roles mapped to their groups as <strong>sync roles</strong> (<code>sync_role_ids</code>); these are managed by sync and reset on every run.</li><li>Users that left all LDAP groups have their sync roles cleared. Local users and service accounts are untouched.</li><li>Unknown group members are created as non-local users with details (email, uid, name) pulled from LDAP.</li></ol><p>Attach more roles to an LDAP group by editing its group map. The management UI shows live LDAP groups with their member counts and mapped roles under <em>LDAP → Group Maps</em>.</p><h3 id="oauth2" tabindex="-1">OAuth2 <a class="header-anchor" href="#oauth2" aria-label="Permalink to “OAuth2”">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/auth/oauth2/authorize</code></td><td>Local authorization endpoint (code flow with consent screen); see <a href="#local-authorize-consent">Local authorize + consent</a>.</td></tr><tr><td><code>GET/POST</code></td><td><code>/auth/oauth2/consent</code></td><td>Consent page for a pending authorize flow (browser plane, needs <code>X-User</code> from the session middleware).</td></tr><tr><td><code>GET</code></td><td><code>/auth/oauth2/auth/{provider}</code></td><td>Start authorization code flow against an upstream provider.</td></tr><tr><td><code>GET</code></td><td><code>/auth/oauth2/code/{provider}</code></td><td>Provider callback; issues a local code.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/token</code></td><td>Token endpoint: <code>password</code>, <code>client_credentials</code>, <code>refresh_token</code>, <code>authorization_code</code> (PKCE supported), <code>urn:ietf:params:oauth:grant-type:device_code</code>, <code>urn:ietf:params:oauth:grant-type:token-exchange</code>, <code>email_code</code>.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/passkey</code></td><td>WebAuthn login: begin without <code>assertion</code>, finish with <code>session_id</code> + <code>assertion</code>; finish issues tokens like the token endpoint.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/device_authorization</code></td><td>RFC 8628 device authorization endpoint; returns <code>device_code</code>, <code>user_code</code>, <code>verification_uri</code>.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/email</code></td><td>Request a one-time email login code (and magic link when <code>redirect_uri</code> is given); always answers 200 to avoid account enumeration.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/signup</code></td><td>Self-registration (requires the <code>signup</code> setting and valid client credentials). With email verification the response is always generic and the account is created at verify time.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/signup/verify</code></td><td>Confirm a signup verification code; creates the local user with the signup default roles. Codes are single use.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/password-reset</code></td><td>Request a password reset mail for local users; always answers 200 to avoid account enumeration.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/password-reset/confirm</code></td><td>Set a new password with a valid reset code (single use, min 8 chars).</td></tr><tr><td><code>POST</code>/<code>GET</code></td><td><code>/auth/oauth2/api-key</code></td><td>Validate a static API key (<code>X-API-Key</code> header or <code>api_key</code> form value) and return identity claims for its principal; no token is issued.</td></tr><tr><td><code>GET</code></td><td><code>/auth/oauth2/certs</code></td><td>JWKS for the auto-generated RS256 signing key.</td></tr><tr><td><code>GET</code></td><td><code>/auth/oauth2/userinfo</code></td><td>Userinfo for a bearer access token.</td></tr><tr><td><code>GET</code></td><td><code>/auth/oauth2/userinfo/{custom}</code></td><td>Userinfo with the named <code>custom_info</code> claim templates applied (add or overwrite claims).</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/revoke</code></td><td>RFC 7009 token revocation. Access tokens are denied by <code>jti</code>; refresh tokens revoke their complete <code>sid</code> session family.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/introspect</code></td><td>RFC 7662 token introspection (client-authenticated); returns <code>active</code> plus token claims.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/register</code></td><td>RFC 7591 dynamic client registration (requires the <code>registration</code> setting).</td></tr><tr><td><code>GET/PUT/DELETE</code></td><td><code>/auth/oauth2/register/{client_id}</code></td><td>RFC 7592 client management with the <code>registration_access_token</code> returned at registration.</td></tr><tr><td><code>GET</code></td><td><code>/auth/oauth2/.well-known/openid-configuration</code></td><td>OpenID configuration built from the request host.</td></tr><tr><td><code>GET</code></td><td><code>/auth/oauth2/.well-known/oauth-authorization-server</code></td><td>RFC 8414 authorization server metadata (same document). Root path-insertion variants <code>/.well-known/oauth-authorization-server[/...]</code> and <code>/.well-known/openid-configuration[/...]</code> are also registered and take effect when the router forwards <code>/.well-known/*</code> to this middleware.</td></tr><tr><td><code>GET</code></td><td><code>/auth/oauth2/openid/{custom}/.well-known/openid-configuration</code></td><td>Same OpenID configuration, but <code>userinfo_endpoint</code> points to <code>/auth/oauth2/userinfo/{custom}</code> so discovery-driven clients pick up that set&#39;s <code>custom_info</code> claims. <code>issuer</code> and the other endpoints stay shared (the <code>issuer</code> matches the <code>id_token</code> <code>iss</code>).</td></tr></tbody></table><p>Token notes:</p><ul><li><p><code>client_credentials</code> authenticates service accounts via the <code>secret</code> detail. With the <code>mtls</code> setting enabled and no secret provided, a client certificate (TLS handshake or trusted proxy header) is matched against the service account&#39;s <code>cert_fingerprint</code> (sha256 of the DER cert) or <code>cert_subject</code> detail.</p></li><li><p><code>password</code> checks local users with bcrypt, or LDAP when the user is not <code>local</code>. Unknown LDAP users are created on first successful sync.</p></li><li><p>The <code>password</code> settings namespace makes those sources explicit and switchable: <code>disabled</code> rejects the grant entirely, <code>local_disabled</code>/<code>ldap_disabled</code> block one source, <code>ldap_register_disabled</code> stops auto-creating unknown users from LDAP. Managed in the UI under <em>OAuth2 → Password Login</em>.</p></li><li><p>Users with a confirmed TOTP secret must send a <code>totp</code> form field on the password grant; a missing code answers <code>401</code> with <code>error=mfa_required</code>. A single-use recovery code is accepted in place of the TOTP code.</p></li><li><p>OAuth clients come from <code>/auth/v1/oauth/clients</code>; service accounts work as a client fallback.</p></li><li><p>Token lifetimes come from the <code>token</code> settings namespace (default access <code>15m</code>, refresh idle <code>24h</code>, remembered-session maximum <code>720h</code> / 30 days).</p></li><li><p>Initial grants accept the <code>remember_me</code> extension parameter. Without it, the original refresh token remains reusable until its fixed <code>refresh_lifetime</code> expiry. With it, refreshes mint a new refresh token with a sliding <code>refresh_lifetime</code> window, capped by the family&#39;s immutable <code>refresh_absolute_lifetime</code> boundary. Refresh requests cannot change this choice; the signed token claim is authoritative.</p></li><li><p>Access, ID and refresh tokens carry a shared <code>sid</code>, <code>auth_time</code> and <code>session_exp</code>. This keeps parallel browser refreshes in one revocable family and gives every family a fixed absolute boundary.</p></li><li><p>An <code>id_token</code> is issued whenever the granted scope contains <code>openid</code>; the <code>nonce</code> from the authorization request is embedded for code-flow logins.</p></li><li><p><strong>PKCE (RFC 7636):</strong> <code>/auth/oauth2/auth/{provider}</code> and <code>/auth/saml/{provider}/login</code> accept <code>code_challenge</code> (+ <code>code_challenge_method</code>, <code>S256</code> or <code>plain</code>); the <code>authorization_code</code> grant then requires a matching <code>code_verifier</code>. With a valid verifier public clients (no stored secret) may exchange codes without a client secret.</p></li><li><p><strong>Redirect whitelist:</strong> authorization and SAML login requests validate <code>redirect_uri</code> against the client&#39;s <code>whitelist_urls</code> (prefix match). Pass <code>client_id</code> to pin a specific client; without one the URI must match some client&#39;s whitelist when at least one is configured. Whitelist-free setups stay open for backwards compatibility. Clients with registered <code>redirect_uris</code> (dynamic registration or manual config) use exact matching instead.</p></li><li><p><strong>Code binding:</strong> authorization codes issued by the local <code>/oauth2/authorize</code> endpoint are bound to the requesting <code>client_id</code> and <code>redirect_uri</code>; the token endpoint rejects redemption by another client or with a different redirect target. Federated codes bind the <code>client_id</code> when the authorization request carried one.</p></li><li><p><strong>Resource indicators (RFC 8707):</strong> <code>resource</code> parameters on <code>/oauth2/authorize</code>, <code>/oauth2/auth/{provider}</code> and the token endpoint land in the access token <code>aud</code> claim (next to <code>turna-auth</code>), so resource servers can require their own identifier. A client record may pin allowed resources with <code>resources</code> (prefix match; empty allows any).</p></li><li><p><strong>Client ID Metadata Documents:</strong> a <code>client_id</code> that is an HTTPS URL with a path (e.g. Claude Code&#39;s <code>https://claude.ai/oauth/claude-code-client-metadata</code>) needs no registration — the document is fetched live (public addresses only, no redirects, 5 KB cap, JSON) and describes a public PKCE client with exact-match <code>redirect_uris</code>. To pin policy for such a client, save a client record <strong>under the URL as its id</strong> (the client API accepts slashes in ids): its <code>resources</code>, <code>scope</code>, <code>skip_consent</code> and <code>roles_claim</code> overlay the live document, while identity and redirect targets stay authoritative in the fetched metadata. If the metadata host is unreachable, a stored record acts as a full fallback (then its own <code>redirect_uris</code>/<code>whitelist_urls</code> apply).</p></li><li><p><strong>Revocation:</strong> access tokens use their <code>jti</code>; revoking a refresh token stores one <code>revoked_session</code> record for its <code>sid</code>, invalidating old and new family members together. Expired flow/revoke rows are purged at startup, hourly, or manually with <code>POST /auth/v1/maintenance/flow-codes/purge</code>.</p></li></ul><h3 id="local-authorize-consent" tabindex="-1">Local authorize + consent <a class="header-anchor" href="#local-authorize-consent" aria-label="Permalink to “Local authorize + consent”">​</a></h3><p><code>GET /auth/oauth2/authorize</code> implements the standard browser-based authorization code flow against local users (enabled by default; the <code>authorize</code> namespace can disable or tune it):</p><ol><li>The client opens <code>/auth/oauth2/authorize?response_type=code&amp;client_id=...&amp;redirect_uri=...&amp;scope=...&amp;state=...&amp;code_challenge=...&amp;code_challenge_method=S256[&amp;resource=...]</code>. Public clients (no stored secret) must send PKCE.</li><li>The request is validated (client, redirect target, PKCE, resources), stored as a pending flow and the browser is redirected to <code>/auth/oauth2/consent?flow=...</code>.</li><li>The consent page needs a logged-in user: it reads <code>X-User</code> set by the <a href="./session.html"><code>session</code></a> middleware in front. List this middleware in session <code>auth_skip_paths: [&quot;&lt;name&gt;&quot;]</code> so the public plane (<code>/auth/oauth2/**</code>, <code>/auth/saml/**</code>, root discovery documents) is skip-pathed; alternatively use session <code>skip_paths: [&quot;/auth/oauth2/**&quot;]</code>. Machine endpoints stay public while cookie-carrying browsers are still authenticated on the consent page. Anonymous browsers are redirected to <code>authorize.login_url</code> (with <code>?redirect_path=</code>) when configured. Clients with <code>skip_consent: true</code> are auto-approved; everyone else sees an approve/deny screen with client name and scopes.</li><li>Approval issues a single-use code bound to client + redirect + PKCE + resources and redirects back to <code>redirect_uri?code=...&amp;state=...</code>; the client exchanges it at the token endpoint with <code>code_verifier</code>.</li></ol><h3 id="mcp-resource-server-integration" tabindex="-1">MCP / resource server integration <a class="header-anchor" href="#mcp-resource-server-integration" aria-label="Permalink to “MCP / resource server integration”">​</a></h3><p>The combination of RFC 8414 metadata, dynamic client registration, PKCE, resource indicators and the consent flow makes the auth middleware a drop-in authorization server for MCP clients (Claude, Cursor, ...):</p><ol><li>Enable registration: <code>PUT /auth/v1/settings/registration {&quot;value&quot;:{&quot;enabled&quot;:true,&quot;client_lifetime&quot;:&quot;720h&quot;}}</code>.</li><li>Route <code>/.well-known/*</code> to the auth middleware so RFC 8414 discovery works from the issuer root.</li><li>Keep the machine endpoints public: list this middleware in session <code>auth_skip_paths: [&quot;&lt;name&gt;&quot;]</code> (the consent page still authenticates cookie-carrying browsers, and <code>authorize.login_url</code> catches anonymous ones). Otherwise add <code>skip_paths: [&quot;/auth/oauth2/**&quot;, &quot;/.well-known/**&quot;]</code> to the <a href="./session.html"><code>session</code></a> middleware in front, or split the router so <code>/auth/oauth2/*</code> bypasses session.</li><li>Protect the MCP endpoint with the <a href="./oauth2_resource.html"><code>oauth2_resource</code></a> middleware, which serves the RFC 9728 protected resource metadata and validates bearer tokens in-process.</li></ol><p>An MCP client then discovers the resource metadata, registers itself (<code>/oauth2/register</code>), sends the user through <code>/oauth2/authorize</code> + consent, and calls the MCP endpoint with the issued bearer token — no manual client setup per user.</p><h3 id="claim-mapping-oauth2-saml-→-roles" tabindex="-1">Claim mapping (OAuth2/SAML → roles) <a class="header-anchor" href="#claim-mapping-oauth2-saml-→-roles" aria-label="Permalink to “Claim mapping (OAuth2/SAML → roles)”">​</a></h3><p>OAuth provider and SAML provider records take an optional <code>claim_mapping</code>, mirroring the LDAP group-sync model:</p><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;claim_mapping&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;roles_claim&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;realm_access.roles&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;use_lmap&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;role_map&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;idp-admin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;admin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]},</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;register&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><code>roles_claim</code> — claim holding group/role values; OAuth2 supports dot paths into nested claims (<code>realm_access.roles</code>, <code>groups</code>), SAML matches the attribute name or friendly name.</li><li><code>use_lmap</code> — resolve claim values through the LDAP group maps (<code>lmaps</code>), sharing one group→role model across LDAP, OAuth2 and SAML.</li><li><code>role_map</code> — map claim values directly to role names or IDs.</li><li><code>register</code> — create unknown users on first login (non-local, like LDAP) with details pulled from the claims.</li></ul><p>Mapped roles land in <code>sync_role_ids</code> and are managed by the provider on every login (dropped at the IdP ⇒ dropped here); manually assigned roles stay untouched. Avoid pointing LDAP sync and a claim mapping at the same users — both manage the same <code>sync_role_ids</code>.</p><h3 id="self-service-account-x-user-plane" tabindex="-1">Self-service account (X-User plane) <a class="header-anchor" href="#self-service-account-x-user-plane" aria-label="Permalink to “Self-service account (X-User plane)”">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/auth/v1/me</code></td><td>Own profile: sanitized details, role/permission names, <code>local</code> flag, TOTP/passkey/api-key overview.</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/me/password</code></td><td>Change own password (<code>current_password</code> + <code>new_password</code>, min 8 chars). Local users only; verified against the stored bcrypt hash.</td></tr><tr><td><code>GET</code></td><td><code>/auth/info</code></td><td>Lighter identity info (details, roles, permissions) — predates <code>/v1/me</code>.</td></tr></tbody></table><p>Together with the other X-User plane routes (<code>/v1/passkey/*</code>, <code>/v1/totp*</code>, <code>/v1/device</code>), this gives users self-service over interactive credentials. Recovery codes: <code>POST /auth/v1/totp/recovery</code> regenerates the set (old codes become invalid). API keys are managed from the admin <em>System -&gt; API Keys</em> page by default; turning on the <code>api_key.self_service</code> setting additionally gives every signed-in user a &quot;Personal access keys&quot; panel on the account page (backed by the owner-scoped <code>/v1/api-keys</code> routes).</p><h3 id="device-flow-rfc-8628" tabindex="-1">Device flow (RFC 8628) <a class="header-anchor" href="#device-flow-rfc-8628" aria-label="Permalink to “Device flow (RFC 8628)”">​</a></h3><p>For CLIs, TVs and other browserless clients:</p><ol><li>The device posts <code>client_id</code> (public clients need no secret) to <code>/auth/oauth2/device_authorization</code> and shows <code>user_code</code> + <code>verification_uri</code> to the user.</li><li>The user, authenticated elsewhere (session sets <code>X-User</code>), approves with <code>POST /auth/v1/device {&quot;user_code&quot;:&quot;XXXX-XXXX&quot;}</code> (or <code>&quot;action&quot;:&quot;deny&quot;</code>); <code>GET /auth/v1/device/{user_code}</code> shows the pending request for a consent page.</li><li>The device polls the token endpoint with <code>grant_type=urn:ietf:params:oauth:grant-type:device_code</code>. Standard errors apply: <code>authorization_pending</code>, <code>slow_down</code>, <code>expired_token</code>, <code>access_denied</code>.</li></ol><h3 id="api-keys" tabindex="-1">API keys <a class="header-anchor" href="#api-keys" aria-label="Permalink to “API keys”">​</a></h3><p>Static long-lived credentials for scripts and integrations, managed as machine principals with an explicit owner user/service account. There is no token exchange: the raw key is sent on every request and validated against the database each time, so revocation is immediate.</p><table tabindex="0"><thead><tr><th>Method</th><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>POST</code></td><td><code>/auth/v1/api-key-principals</code></td><td>Create a key for <code>user_id</code> (<code>name</code>, optional <code>expires_in</code>, <code>role_ids</code>, <code>permission_ids</code>, <code>details</code>). The key (<code>tak_...</code>) is returned exactly once; only the sha256 hash is stored. If role/permission fields are omitted, the owner&#39;s current effective access is snapshotted; if present, requested IDs must be assigned to the owner.</td></tr><tr><td><code>GET</code></td><td><code>/auth/v1/api-key-principals</code></td><td>List all key principals (metadata, owner, role/permission IDs; never the raw key). Optional <code>user_id</code> filters to one owner.</td></tr><tr><td><code>PATCH</code></td><td><code>/auth/v1/api-key-principals/{id}</code></td><td>Update name, <code>role_ids</code>, <code>permission_ids</code>, <code>details</code> or <code>disabled</code>. Changes apply on the next request.</td></tr><tr><td><code>DELETE</code></td><td><code>/auth/v1/api-key-principals/{id}</code></td><td>Revoke a key; access stops immediately.</td></tr><tr><td><code>GET/POST/PATCH/DELETE</code></td><td><code>/auth/v1/api-keys...</code></td><td>X-User owner-scoped plane (personal access keys). Admin-only by default; with the <code>api_key.self_service</code> setting on, any authenticated X-User can issue, list, update and revoke <strong>their own</strong> keys here. A key never carries more than its owner&#39;s roles/permissions. The admin management UI uses <code>api-key-principals</code>.</td></tr></tbody></table><p>Each key is its own principal: identity claims carry <code>sub</code>/<code>preferred_username</code> as <code>api-key:&lt;id&gt;</code>, <code>principal_type=api_key</code>, <code>api_key_id</code>, <code>owner_user_id</code>, plus the key&#39;s own <code>roles</code> and <code>permissions</code> (IDs and names). <code>POST /auth/v1/check</code> accepts <code>api-key:&lt;id&gt;</code> as alias. Validation endpoint: <code>POST /auth/oauth2/api-key</code> with the <code>X-API-Key</code> header returns the claims JSON (<code>401</code> when the key is unknown, disabled, expired, or its owner is disabled).</p><p>Session integration: set <code>api_key: true</code> on the <code>session</code> provider. Session validates <code>X-API-Key</code> directly — in-process when the provider uses <code>auth_middleware</code>, or over <code>oauth2.api_key_url</code> against a remote auth instance — deletes the raw key header, and forwards the claims context and <code>X-User: api-key:&lt;id&gt;</code> downstream.</p><h3 id="token-exchange-rfc-8693" tabindex="-1">Token exchange (RFC 8693) <a class="header-anchor" href="#token-exchange-rfc-8693" aria-label="Permalink to “Token exchange (RFC 8693)”">​</a></h3><p>Confidential clients can exchange a valid access token for a fresh one with <code>grant_type=urn:ietf:params:oauth:grant-type:token-exchange</code> and <code>subject_token=&lt;jwt&gt;</code>. The optional <code>scope</code> parameter narrows the new token; the response carries <code>issued_token_type</code>. Refresh tokens are not exchangeable.</p><h3 id="totp-second-factor" tabindex="-1">TOTP (second factor) <a class="header-anchor" href="#totp-second-factor" aria-label="Permalink to “TOTP (second factor)”">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/auth/v1/totp</code></td><td>Whether the <code>X-User</code> has a confirmed secret.</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/totp/register</code></td><td>Generate a fresh secret; returns the base32 secret and an <code>otpauth://</code> URL.</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/totp/confirm</code></td><td>Verify a code and activate enforcement.</td></tr><tr><td><code>DELETE</code></td><td><code>/auth/v1/totp</code></td><td>Remove the secret.</td></tr></tbody></table><p>Once confirmed, the password grant requires the <code>totp</code> field (see token notes). RFC 6238, SHA1/6 digits/30s — compatible with Google Authenticator and friends.</p><h3 id="email-login-one-time-code-magic-link" tabindex="-1">Email login (one-time code + magic link) <a class="header-anchor" href="#email-login-one-time-code-magic-link" aria-label="Permalink to “Email login (one-time code + magic link)”">​</a></h3><p>Configure the <code>email</code> settings namespace (SMTP relay and templates), then <code>POST /auth/oauth2/email</code> with <code>username=&lt;email&gt;</code> and client credentials. Exchange with <code>grant_type=email_code</code> + <code>code=...</code> — codes are single use and unknown addresses are never revealed.</p><p>The one-time code and the magic link are two independent mails:</p><ul><li><strong>One-time code:</strong> controlled by <code>disabled</code> with its own <code>subject</code> / <code>body_template</code>. Sent whenever email login is enabled.</li><li><strong>Magic link:</strong> controlled by <code>magic_link</code> (default <code>true</code>) with its own <code>magic_link_subject</code> / <code>magic_link_body_template</code>. Only produced when the request carries a <code>redirect_uri</code> allowed by the client&#39;s <code>whitelist_urls</code>; the link is <code>redirect_uri?code=...</code>. Turn <code>magic_link</code> off to send the code only — useful when the same SMTP relay is shared with the <code>signup</code> flows and you don&#39;t want login magic links.</li></ul><p>All subject/body values are Go <code>text/template</code>. Available fields: <code>.Email</code>, <code>.Name</code>, <code>.Code</code>, <code>.MagicLink</code>, <code>.ExpiresIn</code>, <code>.ClientID</code>, <code>.RedirectURI</code>, <code>.UserID</code>, <code>.UserAlias</code>. <code>POST /auth/v1/email/preview</code> renders an unsaved settings payload (with or without <code>redirect_uri</code>) so the UI can preview and validate either mail before saving.</p><h3 id="signup-and-password-reset" tabindex="-1">Signup and password reset <a class="header-anchor" href="#signup-and-password-reset" aria-label="Permalink to “Signup and password reset”">​</a></h3><p>The <code>signup</code> settings namespace enables optional self-registration and forgot-password flows, all managed from the UI:</p><ol><li><code>POST /auth/oauth2/signup</code> with client credentials, <code>email</code>, <code>password</code> (min 8) and optional <code>name</code>/<code>redirect_uri</code>. With <code>email_verification</code> (default) the pending registration is stored as a hashed flow code — the password is kept bcrypt-hashed, never plain — and a verification mail is sent; the response never reveals whether the address is registered. Without verification the local user is created immediately (duplicates answer <code>409</code>).</li><li><code>POST /auth/oauth2/signup/verify</code> with the mailed <code>code</code> creates the user: <code>local: true</code>, alias = email, <code>default_role_ids</code> granted.</li><li><code>POST /auth/oauth2/password-reset</code> with <code>email</code> mails a reset code to local users (always <code>200</code>); <code>POST /auth/oauth2/password-reset/confirm</code> with <code>code</code> + <code>password</code> sets the new password. Non-local (LDAP/federated) users are skipped — their password lives upstream.</li></ol><p>Mails use the <code>email</code> SMTP relay; magic links are appended to <code>redirect_uri</code> when it passes the client&#39;s <code>whitelist_urls</code>. Verify/reset subject and body templates are Go <code>text/template</code> strings (same fields as email login plus <code>.Name</code>), validated on save and previewable in the UI. The <a href="./login.html"><code>login</code></a> middleware picks these flows up automatically and shows &quot;Create account&quot; / &quot;Forgot password?&quot; on the login page.</p><h3 id="saml-2-0-service-provider" tabindex="-1">SAML 2.0 (service provider) <a class="header-anchor" href="#saml-2-0-service-provider" aria-label="Permalink to “SAML 2.0 (service provider)”">​</a></h3><p>SAML IdPs (ADFS, Azure AD, Okta, Shibboleth, ...) are stored like other encrypted config records:</p><table tabindex="0"><thead><tr><th>Method</th><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET/PUT/DELETE</code></td><td><code>/auth/v1/saml/providers</code>, <code>/auth/v1/saml/providers/{id}</code></td><td>SAML provider records.</td></tr><tr><td><code>GET</code></td><td><code>/auth/saml/{provider}/metadata</code></td><td>SP metadata XML to register at the IdP.</td></tr><tr><td><code>GET</code></td><td><code>/auth/saml/{provider}/login</code></td><td>Start a login; <code>redirect_uri</code> required, <code>state</code>/<code>scope</code> optional.</td></tr><tr><td><code>POST</code></td><td><code>/auth/saml/{provider}/acs</code></td><td>Assertion consumer service (IdP POST binding callback).</td></tr></tbody></table><p>Provider config keys: <code>metadata_url</code> or inline <code>metadata_xml</code>, optional <code>entity_id</code>, <code>alias_attribute</code> (default: email-like attributes, then the subject NameID) and <code>sign_requests</code> (RSA-SHA256 with the auto-generated SP key from the <code>saml</code> settings namespace). After the assertion is validated the user is redirected to <code>redirect_uri?code=...&amp;state=...</code> and the client exchanges the code with the standard <code>authorization_code</code> grant — same shape as the upstream OAuth2 provider flow.</p><h3 id="mtls-client-credentials" tabindex="-1">mTLS client credentials <a class="header-anchor" href="#mtls-client-credentials" aria-label="Permalink to “mTLS client credentials”">​</a></h3><p>mTLS uses service accounts as clients:</p><ol><li>Enable the <code>mtls</code> setting namespace (<code>enabled: true</code>). If TLS terminates at a proxy, set <code>cert_header</code> to the trusted header carrying the URL-escaped PEM/base64 DER certificate.</li><li>Create or edit a service account. Its first alias is the OAuth2 <code>client_id</code>.</li><li>Fill <code>details.cert_fingerprint</code> (sha256 DER hex, recommended) or <code>details.cert_subject</code> on that service account. The UI can calculate the fingerprint from a pasted PEM certificate.</li><li>Request <code>/auth/oauth2/token</code> with <code>grant_type=client_credentials</code> and <code>client_id=&lt;alias&gt;</code> while presenting the certificate. A client secret is not required for mTLS-only clients.</li></ol><p>Session integration is token based: mTLS authenticates the token request, not the session middleware directly. Use the issued access token as a bearer token on routes protected by <code>session</code>.</p><h3 id="encrypted-config" tabindex="-1">Encrypted config <a class="header-anchor" href="#encrypted-config" aria-label="Permalink to “Encrypted config”">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/auth/v1/info</code></td><td>Prefix, storage type, and current auth version.</td></tr><tr><td><code>GET</code></td><td><code>/auth/v1/capabilities</code></td><td>Current request capabilities (<code>is_admin</code>, <code>anonymous_admin</code>, configured admin permission). The UI uses this to hide admin pages from normal users.</td></tr><tr><td><code>GET/PUT/DELETE</code></td><td><code>/auth/v1/settings</code>, <code>/auth/v1/settings/{namespace}</code></td><td>Encrypted JSON settings. Writes apply immediately on the handling instance; the <code>jwt</code> namespace is validated (parseable private key + kid) before saving.</td></tr><tr><td><code>GET</code></td><td><code>/auth/v1/session-providers</code></td><td>The UI-managed session middleware provider list (<code>session_providers</code> namespace) with the auth version in <code>meta.version</code>. Remote turna instances poll it through the session middleware&#39;s <code>provider_source.url</code>; admin-protected because provider client secrets travel in the payload.</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/custom-info/preview</code></td><td>Render an unsaved <code>custom_info</code> set against sample claims and user details; returns the resulting claims and validates the templates (used by the UI).</td></tr><tr><td><code>POST</code></td><td><code>/auth/v1/jwt/rotate</code></td><td>Generate and activate a fresh RSA signing key (new <code>kid</code>); outstanding tokens become invalid.</td></tr><tr><td><code>GET/PUT/DELETE</code></td><td><code>/auth/v1/oauth/clients</code>, <code>/auth/v1/oauth/clients/{id}</code></td><td>OAuth client records.</td></tr><tr><td><code>GET/PUT/DELETE</code></td><td><code>/auth/v1/oauth/providers</code>, <code>/auth/v1/oauth/providers/{id}</code></td><td>OAuth provider records.</td></tr><tr><td><code>GET/PUT/DELETE</code></td><td><code>/auth/v1/ldap/configs</code>, <code>/auth/v1/ldap/configs/{id}</code></td><td>LDAP config records.</td></tr></tbody></table><p>Config example:</p><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;enabled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;config&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;client_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;turna&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;client_secret&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;secret&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;scopes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;openid&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="passkeys-webauthn" tabindex="-1">Passkeys (WebAuthn) <a class="header-anchor" href="#passkeys-webauthn" aria-label="Permalink to “Passkeys (WebAuthn)”">​</a></h3><p>Passkey support uses the dependency-free engine from <code>github.com/rakunlabs/ada/middleware/auth/strategy/passkey</code>. Credentials are stored in <code>auth_passkey_credentials</code>; in-flight challenges use the OAuth2 code store (<code>cache.code_store</code>), so multi-instance deployments should switch it to Redis.</p><table tabindex="0"><thead><tr><th>Method</th><th>Route</th><th>Purpose</th></tr></thead><tbody><tr><td><code>POST</code></td><td><code>/auth/v1/passkey/register</code></td><td>Begin/finish registration (begin without <code>credential</code>, finish with <code>session_id</code> + <code>credential</code>). Without <code>user_id</code> targets the <code>X-User</code> identity (self-service); with <code>user_id</code> registers for that user and requires admin capability.</td></tr><tr><td><code>GET</code></td><td><code>/auth/v1/passkey/credentials</code></td><td>List passkeys for the <code>X-User</code> identity. Listing another <code>user_id</code> requires admin capability.</td></tr><tr><td><code>DELETE</code></td><td><code>/auth/v1/passkey/credentials/{id}</code></td><td>Delete a stored passkey.</td></tr><tr><td><code>POST</code></td><td><code>/auth/oauth2/passkey</code></td><td>Public login ceremony; finish responds with the standard token JSON.</td></tr></tbody></table><p>Login requests carry <code>client_id</code>/<code>client_secret</code> like the password grant; <code>username</code> scopes <code>allowCredentials</code> to a known user, empty uses the discoverable (passwordless) flow. <code>allowCredentials</code> and <code>excludeCredentials</code> include the transports captured at registration (<code>internal</code>, <code>hybrid</code>, <code>usb</code>, ...) so browsers surface the matching authenticator — a synced browser-profile passkey shows up directly instead of the generic QR/security-key dialog. The management UI shows registered passkeys on the user page; admins can enroll a passkey for the selected user, which binds the authenticator present in the operator&#39;s browser to that user&#39;s account. The self-service Account page enrolls for the signed-in user.</p><h2 id="session-login-integration" tabindex="-1">Session/login integration <a class="header-anchor" href="#session-login-integration" aria-label="Permalink to “Session/login integration”">​</a></h2><p>The middleware registers itself as an in-process token issuer under its middleware name. A <a href="./session.html"><code>session</code></a> provider can reference it with <code>auth_middleware</code> so JWT validation (key lookup) and refresh happen in-process — no <code>cert_url</code>/<code>token_url</code> self-calls:</p><div class="language-yaml"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">middlewares</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  auth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    auth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      database</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">        dsn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">postgres://...</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      encryption</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">        key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\${AUTH_ENCRYPTION_KEY}</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      store</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">        active</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">redis</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">        redis</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">          address</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;redis:6379&quot;</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">        turna</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">          auth_middleware</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auth&quot;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # middleware key above</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">          password_flow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">          passkey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">          oauth2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">            client_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ui&quot;</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">         # OAuth client registered in auth</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">            scopes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;openid&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">        token</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">          login_path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/login/&quot;</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  login</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    login</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">        base</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/login/&quot;</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      session_middleware</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;session&quot;</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">routers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  login</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/login/*</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    middlewares</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">login</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  auth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/auth/*</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    middlewares</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">auth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]   </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># session sets X-User for the auth API/UI</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/*</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    middlewares</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">session</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>With this wiring the <a href="./login.html"><code>login</code></a> page authenticates users against the auth middleware (password grant and/or passkey), the session middleware stores tokens server-side and validates them with the auth signing key, and the auth management API/UI receives the authenticated <code>X-User</code> header.</p><p>Set the <code>admin</code> runtime namespace to split normal self-service users from operators:</p><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;permission&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;turna.auth.admin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;allow_missing_x_user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>When <code>permission</code> is set, requests with <code>X-User</code> must have that permission (by ID or name) to call management APIs or see admin UI tabs. Non-admin users still reach self-service (<code>#account</code>, <code>#device</code>). If <code>X-User</code> is missing and <code>allow_missing_x_user</code> is true, auth treats the request as break-glass admin; this is intended for local recovery after removing the session chain, not for public exposure. Empty <code>permission</code> keeps bootstrap-open behavior until you create the first admin permission/role.</p><h3 id="remote-deployment" tabindex="-1">Remote deployment <a class="header-anchor" href="#remote-deployment" aria-label="Permalink to “Remote deployment”">​</a></h3><p>The auth middleware can also run on a separate turna instance. Since all issuer endpoints are plain HTTP, session/login on other instances connect like to any OAuth2 IdP — drop <code>auth_middleware</code> and use URLs instead:</p><div class="language-yaml"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  turna</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    password_flow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    passkey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    oauth2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      client_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ui&quot;</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      scopes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">openid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      cert_url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://auth.example.com/auth/oauth2/certs</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      token_url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://auth.example.com/auth/oauth2/token</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      passkey_url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://auth.example.com/auth/oauth2/passkey</span></span></code></pre></div><p>Remote notes:</p><ul><li>Keep <code>/auth/oauth2/*</code> publicly routable on the auth instance (no <code>session</code> in front); normally protect <code>/auth/v1/*</code> and <code>/auth/ui/*</code> with a session chain. If you intentionally remove the session chain for recovery, <code>admin.allow_missing_x_user=true</code> grants break-glass admin access to requests without <code>X-User</code>.</li><li>Set the <code>passkey</code> runtime settings (<code>rp_id</code>, <code>origins</code>) when login pages are served from an unrelated domain; the default derives <code>rp_id</code> as the registrable domain (eTLD+1) of the forwarded host, so auth and login pages on sibling subdomains (<code>auth.example.com</code>, <code>app.example.com</code>) already share one passkey scope without configuration.</li><li>The session middleware fetches JWKS from <code>cert_url</code> at startup, so the auth instance must be reachable when dependent instances boot.</li></ul><h2 id="migration-from-iam-oauth2" tabindex="-1">Migration from iam/oauth2 <a class="header-anchor" href="#migration-from-iam-oauth2" aria-label="Permalink to “Migration from iam/oauth2”">​</a></h2><ul><li><code>iam</code> and <code>oauth2</code> middlewares are deprecated but still available for Badger-backed deployments.</li><li>API payload shapes match the old IAM API (<code>data.Response</code>, <code>UserExtended</code>, etc.), so existing clients largely work after switching base paths to <code>/auth/v1</code>.</li><li>Not ported: Badger binary backup/restore endpoints (<code>/v1/backup</code>, <code>/v1/restore</code>) and the Badger write-api/Redis sync model; PostgreSQL with version polling replaces them.</li></ul>`,103)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};