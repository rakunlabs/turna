import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.CxcmDv9y.js";var i=JSON.parse(`{"title":"compress","description":"","frontmatter":{},"headers":[],"relativePath":"reference/server/http/middlewares/compress.md","filePath":"reference/server/http/middlewares/compress.md","lastUpdated":1790374191000}`),a={name:`reference/server/http/middlewares/compress.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="compress" tabindex="-1">compress <a class="header-anchor" href="#compress" aria-label="Permalink to “compress”">​</a></h1><p><code>compress</code> compresses HTTP responses with <code>zstd</code>, <code>br</code> (Brotli) or <code>gzip</code>, chosen from the client&#39;s <code>Accept-Encoding</code> header.</p><div class="language-yaml"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  http</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    middlewares</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">      compress</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">        compress</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {}</span></span></code></pre></div><h2 id="fields" tabindex="-1">Fields <a class="header-anchor" href="#fields" aria-label="Permalink to “Fields”">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>encodings</code></td><td><code>[zstd, br, gzip]</code></td><td>Enabled encodings in server preference order.</td></tr><tr><td><code>min_length</code></td><td><code>1024</code></td><td>Responses smaller than this (bytes) are sent uncompressed.</td></tr><tr><td><code>excluded_content_types</code></td><td>see below</td><td>Content types that are never compressed. <code>type/*</code> wildcards are supported.</td></tr><tr><td><code>included_content_types</code></td><td></td><td>When set, only these content types are compressed.</td></tr><tr><td><code>gzip_level</code></td><td><code>-1</code> (default)</td><td>gzip level, <code>1</code>-<code>9</code>.</td></tr><tr><td><code>brotli_level</code></td><td><code>4</code></td><td>Brotli quality, <code>0</code>-<code>11</code>. Use higher values only for cached static content.</td></tr><tr><td><code>zstd_level</code></td><td><code>3</code></td><td>zstd level, <code>1</code>-<code>22</code>, mapped to the closest encoder speed.</td></tr></tbody></table><h2 id="encoding-selection" tabindex="-1">Encoding selection <a class="header-anchor" href="#encoding-selection" aria-label="Permalink to “Encoding selection”">​</a></h2><p>The encoding with the highest <code>q</code> value in <code>Accept-Encoding</code> wins. When several share the same quality, the order of <code>encodings</code> decides. <code>*</code> matches every enabled encoding and <code>q=0</code> disables one.</p><table tabindex="0"><thead><tr><th>Accept-Encoding</th><th>Result</th></tr></thead><tbody><tr><td><code>gzip, deflate, br, zstd</code></td><td><code>zstd</code></td></tr><tr><td><code>gzip, br</code></td><td><code>br</code></td></tr><tr><td><code>gzip;q=1, br;q=0.5</code></td><td><code>gzip</code></td></tr><tr><td><code>identity</code></td><td>not compressed</td></tr></tbody></table><h2 id="skipped-responses" tabindex="-1">Skipped responses <a class="header-anchor" href="#skipped-responses" aria-label="Permalink to “Skipped responses”">​</a></h2><p>Responses are passed through unchanged when:</p><ul><li>the request is a <code>HEAD</code>, <code>Range</code> or WebSocket upgrade request,</li><li>the status is <code>1xx</code>, <code>204</code>, <code>206</code> or <code>304</code>,</li><li>the response already has <code>Content-Encoding</code> or <code>Content-Range</code>,</li><li><code>Cache-Control</code> contains <code>no-transform</code>,</li><li>the content type is excluded,</li><li>the body is smaller than <code>min_length</code>.</li></ul><p>The default excluded types are already compressed formats: <code>image/*</code> (except <code>image/svg+xml</code>, <code>image/bmp</code> and icons), <code>video/*</code>, <code>audio/*</code>, <code>font/woff</code>, <code>font/woff2</code>, archive types, <code>application/octet-stream</code> and <code>application/grpc</code>. Setting <code>excluded_content_types</code> replaces this list.</p><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to “Notes”">​</a></h2><ul><li><code>Vary: Accept-Encoding</code> is always added.</li><li>A strong <code>ETag</code> is converted to a weak one when the body is compressed.</li><li><code>Accept-Encoding</code> is removed from the request before calling the next handler, so a proxied upstream sends an uncompressed body and turna compresses it once.</li><li>Flushed (streaming / SSE) responses are compressed and flushed immediately.</li></ul><p>Use <a href="./decompress.html"><code>decompress</code></a> to decode compressed request bodies.</p>`,15)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};